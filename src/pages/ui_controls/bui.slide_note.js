loader.define(function(require,exports,module) {

    // 快速初始化
    var uiSlide = bui.slide({
        id:"#slideNote",
        height:100,
        autoplay: true,
        zoom: true,
        direction: "y"
    })
})
